on: [push]

jobs:
  build_docker_image:
    runs-on: ubuntu-latest
    name: Builds Docker image to run tests for ros_messaging
    steps:
      - name: Checkout
      uses: actions/checkout@v2
      - name: Build Docker image
        uses: ./